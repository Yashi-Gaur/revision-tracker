// Assemble all sections into the CURRICULUM object used by app.js.
// To add a new section: create a new file here, define its const, then add it below.
const CURRICULUM = {
  dsa:          DSA_SECTION,
  frontend:     FRONTEND_SECTION,
  backend:      BACKEND_SECTION,
  lld:          LLD_SECTION,
  systemDesign: SYSTEM_DESIGN_SECTION,
};
